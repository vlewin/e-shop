%h4.page-header.no-margin
  Products
  .pull-right
    %small= link_to 'New product', new_product_path

.panel.panel-default
  .panel-body
    %table.table.table-hover
      %thead
        %tr
          %th Image
          %th EAN
          %th Name
          %th Quantity
          %th Price
          %th VAT
          %th{ colspan: 3, width: '20%' }

      %tbody
        - @products.each do |product|
          %tr
            %td= product_image_tag product
            %td= product.ean
            %td= product.name
            %td= product.quantity
            %td= number_to_currency product.price
            %td= number_to_percentage product.vat
            %td.text-right
              = icon_link_to '', product, { class: 'btn btn-sm btn-default' }, 'fa-eye'
              = icon_link_to '', edit_product_path(product), { class: 'btn btn-sm btn-default' }, 'fa-edit'
              = icon_link_to '', product, { data: {confirm: 'Are you sure?'}, method: :delete, class: 'btn btn-sm btn-default' }, 'fa-trash-o'

